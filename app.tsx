// src/App.tsx
import React from 'react';

// Define the component as a functional component (React.FC is optional but common)
const App: React.FC = () => {
  return (
    <div className="App">
      <h1>Welcome to my App!</h1>
      <p>Edit `src/App.tsx` and save to reload.</p>
    </div>
  );
}

export default App;
